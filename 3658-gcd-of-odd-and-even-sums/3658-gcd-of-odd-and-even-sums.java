class Solution {
    public int gcdOfOddEvenSums(int n) {
        return n;
        // int odds=n*n;
        // int evens=n*(n+1);
        
        // int min=Math.min(evens,odds);

        // for(int i=min/2; i>0; i--)
        // {
        //     if(evens%i == 0 && odds%i == 0)
        //     return i;
        // }
        // return 1;
    }
}